#include <iostream>
using namespace std;

int main() {
   char a;
   char b;
   char c;

   cin >> a;
   cin >> b;
   cin >> c;

   char pain[]{ a, b, c };

   for (auto i{0}; i < 2; ++i) {
      char ichar{pain[i]};
		for (auto )
   }

   cout << endl;

   return 0;
}
